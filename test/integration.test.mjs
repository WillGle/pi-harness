import test from "node:test"; import assert from "node:assert/strict"; import { readFileSync } from "node:fs";
test("package declares Pi resources, the selected coordinator extension, and a pinned peer runtime", () => { const pkg = JSON.parse(readFileSync("package.json", "utf8")); assert.ok(pkg.pi.extensions.includes("extensions")); assert.ok(pkg.pi.extensions.includes("node_modules/@tintinweb/pi-subagents/dist/index.js")); assert.equal(pkg.dependencies["@tintinweb/pi-subagents"], "0.19.0"); assert.equal(pkg.peerDependencies["@earendil-works/pi-coding-agent"], "0.87.1"); });
test("ACP bridge exposes the required command contract", () => { const source = readFileSync("packages/pi-harness-acp/bin/pi-harness-acp.mjs", "utf8"); for (const command of ["plan", "goal", "skill-hub", "initialize", "session/cancel"]) assert.match(source, new RegExp(command)); });
test("project memory writes require the explicit learn command", () => { const source = readFileSync("extensions/pi-harness.ts", "utf8"); assert.match(source, /registerCommand\?\.\("learn"/); assert.doesNotMatch(source, /name:\s*"pi_harness_learn"/); });
