# Investigator receipt example (not an agent definition)

The Coordinator may dispatch a read-only `scout` or `research` profile through `pi_harness_coordinate`. This file does not register an agent or authorize another spawn. In L3, the ExecutionUnit may report paths and line numbers with exact technical identifiers. If a condition or uncertainty matters, state it in a full sentence. This receipt is not a TaskResult and cannot be promoted without Harness normalization.

Example:

```
Defs:
- lib/state.mjs:20 — `goalState` — defines Mission state.
Evidence: lib/state.mjs:20
```
