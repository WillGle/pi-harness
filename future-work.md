# Future Work

## Upstream ACP consolidation

Why:
Replace the local ACP bridge only if upstream exposes extension-command projection without copied lifecycle logic.

Trigger:
Upstream provides a supported hook for `/plan`, `/goal`, `/learn`, and `/skill-hub` while preserving current session/load/cancel behavior.
