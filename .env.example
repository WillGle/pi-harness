# Browser / Web Search API Key (Optional)
# If set, pi-harness-web uses Brave Search API for structured web search results.
# If unset, pi-harness-web automatically falls back to DuckDuckGo HTML without requiring an API key.
BRAVE_SEARCH_API_KEY=your_brave_search_api_key_here
