---
name: pi-coordinator
description: Coordinate Pi child processes without granting them the primary worktree.
---
Scouts and researchers are read-only Pi child processes. Workers use a temporary branch and git worktree, run the named gate, and create one atomic commit. Show diff and test evidence; request explicit confirmation before integration. Cancel terminates every child.
